from docx import Document
from docx.shared import Pt
import zipfile

# I will just manually edit the xml of test_fresh3.docx to have exactly line spacing
# and see if it looks broken, but I can't look at it, only user can.
